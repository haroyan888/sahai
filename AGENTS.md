このプロジェクトでエージェントに共通で指示する内容をここに記述します。

## ブランチ運用方針

このプロジェクトでは、変更単位（機能追加・修正・リファクタなど）ごとに必ず作業用ブランチを切って作業を行います。

- mainブランチで直接作業（コミット）しない
- 1つの変更単位につき1つのブランチを作成する
- 作業（変更単位）ごとにコミットを行う

## コミットメッセージの書き方

コミットメッセージは以下の形式に従ってください。

```
<種別>: <変更内容の要約（日本語）>

<変更理由や背景の説明（1〜2文、日本語）>

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

- 1行目は `docs:` `feat:` `fix:` `refactor:` `build:` などの種別プレフィックスに続けて、変更内容を日本語で簡潔に要約する
- 本文には「何を」ではなく「なぜ」変更したのかを日本語で記述する
- 末尾に `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` を付与する
